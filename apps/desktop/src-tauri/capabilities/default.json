{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "B08 main window with core defaults and the narrow destroy permission required after a successful close-time save.",
  "windows": ["main"],
  "permissions": ["core:default", "core:window:allow-destroy"]
}
